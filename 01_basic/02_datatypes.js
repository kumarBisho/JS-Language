"use strict"; // treat all js code as a newer version of js

// alert(3 + 4) -> it can use only in browser in documented format. Here in node we can't use it.

let name = "Bisho" // this is a string
let age = 34  // this is a number. its range are 2 to the power 53
let login = false // this is boolean type . true/false
let job = null // it is a specific type of standalone value
let state // this is undefined=> value not given(assigned

// symbal => this is used to get unique structure 

console.log(typeof name)
console.log(typeof null) // null is a object type. null itself is not a type
console.log(typeof undefined) // undefined itself is a type of datatype
